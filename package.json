{
  "name": "shiminger.com",
  "version": "1.0.0",
  "description": "Nuxt.js Vuetify project",
  "author": "Lerte Smith<lerte@qq.com>",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "push": "push-dir --dir=public --branch=gh-pages --cleanup --force --verbose",
    "test": "echo \"Error: no test specified\" && exit 0"
  },
  "dependencies": {
    "@nuxtjs/pwa": "^2.6.0",
    "@nuxtjs/vuetify": "^0.4.3",
    "nuxt": "^2.2.0",
    "qrcode.vue": "^1.6.1"
  },
  "devDependencies": {
    "push-dir": "^0.4.1",
    "stylus": "^0.54.5",
    "stylus-loader": "^3.0.2"
  }
}
